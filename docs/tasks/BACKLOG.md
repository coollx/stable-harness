# Backlog

Deferred ideas and directions. One line to one paragraph each: the idea, an optional trigger clause, a provenance date. Future tense only — a made choice is a decision record, not a backlog entry. Entries graduate when a session is spent on them (`/task plan` consumes the entry and deletes it); pruning and deletion are expected. Harness friction is recorded here as `harness:` lines and consumed by the next `/meta` session.

- 2026-09-17 · Will decide what stands in for future learning ability at edit time: a learned critic that predicts it, a directly measured disorder score computed from the harness files, or both. Trigger: once the continual-learning reading is done (plasticity, backward and forward transfer, intransigence, dormant units, software evolution laws). The outcome will become a decision record and a framing.md amendment. Whatever stands in, its correlation with stream decline will be analyzed carefully before it is read as a cause: Understanding-Plasticity found no direct predictor of plasticity loss in networks (dead units, weight norm and rank all reverse sign across settings), and its sign-reversal test is one tool for that analysis, not the methodology we will follow verbatim. Provenance: framing discussion 2026-09-15 to 2026-09-17, following the seven memory-paper reads; Understanding-Plasticity reading 2026-09-17.
- 2026-09-17 · Will build the task stream as recurring chunks of unrelated domains (for example coding, math, ALFWorld), each chunk about 10 instances drawn at random within its domain and never reused, long enough to show decline; and will read plasticity as the gap between the current harness and a no-history reference harness scored on the same task, following Loss-of-Plasticity's from-scratch comparison. Trigger: once the continual-learning reading is done. Provenance: Loss-of-Plasticity reading 2026-09-17.
- 2026-09-19 · May measure harness plasticity at stream checkpoints with a fixed-budget probe adapted from Understanding-Plasticity: copy the harness, run the harness agent on probe tasks drawn from outside the current batch for a fixed number of task rounds (one task scored, then one edit opportunity), report the probe score minus the same measurement from the base harness. If used, the budget will be counted in task rounds rather than agent actions, since actions differ in size and every harness paper in refs counts rounds; edit count would be logged as cost only. The budget definition is still open and other measurement methods will be considered. Trigger: same as the line above. Provenance: Understanding-Plasticity reading 2026-09-17, discussion 2026-09-19.
- 2026-09-23 · Will design the first stream experiment with vanilla and one or more self-improving harness baselines, our own method joining later as a third category; which baselines and which measures will be decided in the design session, and harness intransigence, which has no measure yet, will be designed there. Provenance: AgentStream reading 2026-09-23.
