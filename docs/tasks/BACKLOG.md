# Backlog

Deferred ideas and directions. One line to one paragraph each: the idea, an optional trigger clause, a provenance date. Future tense only — a made choice is a decision record, not a backlog entry. Entries graduate when a session is spent on them (`/task plan` consumes the entry and deletes it); pruning and deletion are expected. Harness friction is recorded here as `harness:` lines and consumed by the next `/meta` session.

- 2026-09-17 · Will decide what stands in for future learning ability at edit time: a learned critic that predicts it, a directly measured disorder score computed from the harness files, or both. Trigger: once the continual-learning reading is done (plasticity, backward and forward transfer, intransigence, dormant units, software evolution laws). The outcome will become a decision record and a framing.md amendment. Provenance: framing discussion 2026-09-15 to 2026-09-17, following the seven memory-paper reads.
- 2026-09-17 · Will build the task stream as recurring chunks of unrelated domains (for example coding, math, ALFWorld), each chunk about 10 instances drawn at random within its domain and never reused, long enough to show decline; and will read plasticity as the gap between the current harness and a no-history reference harness scored on the same task, following Loss-of-Plasticity's from-scratch comparison. Trigger: once the continual-learning reading is done. Provenance: Loss-of-Plasticity reading 2026-09-17.
